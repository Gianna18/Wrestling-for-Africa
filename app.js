function ageOfMinors(age) {
    if (age < 18) {
       console.log('Don\'t drink.'); 
    } else if (age < 21) {
       console.log('Dont\'t work.'); 
    } else (age > 21); {
        console.log('You can do as you please.');
    }
}
ageOfMinors (19);
ageOfMinors (25);
ageOfMinors (16)

function kidsNames(names) {
    for (let nam =nam < array.lengnam++) {
        const element = arnam];
        
    }
}